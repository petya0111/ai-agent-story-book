# Copy this file to .env.docker (do NOT commit .env.docker with real secrets)
# Example credentials for local development only.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=<password>
POSTGRES_DB=book_agent

# Optional: Prisma will use DATABASE_URL from your usual .env file
# You can set DATABASE_URL in backend/.env to:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/book_agent?schema=public"