# PostgreSQL connection for Prisma
DATABASE_URL="postgresql://user:password@localhost:5432/book_agent?schema=public"

# OpenAI / ChatGPT API key (server-side only)
OPENAI_API_KEY="sk-REPLACE_ME"

# Path to the book PDF to ingest at server startup (optional for MVP)
BOOK_PDF_PATH="./data/napoleon-hill-think-and-grow-rich.pdf"

# Optional: model name and generation temperature
OPENAI_MODEL="gpt-4o-mini"
GENERATE_TEMPERATURE=0.4

# Server port
PORT=4000